# 第3回　種々のデータ／基本統計量（代表値）

対応節：1.2.1, 1.2.2a

## 講義スライド案（25枚）

**スライド1｜本日のねらい**
- データにはさまざまな種類・尺度があることを理解する
- データの「代表値」（平均・中央値・最頻値など）の意味と使い分けを学ぶ
- 尺度水準によって使える代表値が異なることを知る
- 実データを使いながら代表値の計算・解釈に慣れる
- 次回以降の「散らばりの指標」につながる土台を作る

**スライド2｜1.2 データの記述：全体像**
- 本節では、データを適切に要約・記述するための指標を学ぶ
- まず「種々のデータ」としてデータの特徴づけ方を整理する
- 続いて1変数データの要約（代表値・散らばりの指標）を学ぶ
- 今後、2変数データの要約（相関など）にもつなげていく
- 今日はその入り口となる1.2.1と1.2.2の一部を扱う

**スライド3｜データ・変数とは何か**
- 観測対象について観測された数値・記号を「データ」と呼ぶ
- データが表す属性のことを「変数」と呼ぶ
- 例：ある人の身長という「変数」に対する観測値が「データ」
- 数値で与えられるデータには連続的な値と離散的な値がある
- これらの違いが「連続データ」「離散データ」という分類

**スライド4｜連続データと離散データ／変数の数による分類**
- 連続データ：連続的な値をとるデータ（例：身長、気温）
- 離散データ：とびとびの値をとるデータ（例：人数、回数）
- 1つの変数からなるデータ＝1変数データ
- 2つの変数の組からなるデータ＝2変数データ
- 複数の変数の組からなるデータ＝多変数データ

**スライド5｜尺度水準①：名義尺度・順序尺度**
- データは「尺度水準」という性質によって4つに分類される
- 名義尺度：順序に意味のない数値・記号（例：血液型、性別）
- 名義尺度では等値関係のみに意味があり、大小関係には意味がない
- 順序尺度：順序関係に意味のある数値・記号（例：満足度の3段階評価）
- 順序尺度は「順位」はわかるが「差の大きさ」には意味がない

**スライド6｜尺度水準②：間隔尺度・比率尺度**
- 間隔尺度：順序に加えて「差（間隔）」に意味があるデータ
- 間隔尺度は原点の取り方に任意性がある（例：摂氏温度）
- 比率尺度：順序・差に加えて「原点」も定まるデータ
- 比率尺度は原点が固定される（例：身長、体重、金額）
- 比率尺度は比例尺度と呼ばれることもある

**スライド7｜Memo：摂氏・華氏・ケルビン温度**
- 摂氏温度・華氏温度は間隔尺度の代表例
- 「5℃高い」は意味を持つが「2倍の気温」は意味を持たない
- 摂氏・華氏の原点（0度の位置）は歴史的な経緯で任意に定められた
- 一方、ケルビン温度は原点（絶対零度）が物理的に定まる比率尺度
- 同じ「温度」でも尺度水準が異なる点に注意が必要

**スライド8｜尺度水準のまとめと質的・量的データ**
- 名義尺度が最も低い尺度水準、比率尺度が最も高い尺度水準
- 高い尺度水準のデータは、低い尺度水準として扱うことも可能
- 名義尺度・順序尺度データ＝質的データ
- 間隔尺度・比率尺度データ＝量的データ（数値データ）
- 量的データはさらに計量データと計数データに分けられる

**スライド9｜まとめ：1.2.1a データの分類**
- データは変数の数、連続・離散、尺度水準などで特徴づけられる
- 尺度水準には名義・順序・間隔・比率の4種類がある
- どの尺度として扱うかは分析の目的や手法によって判断が変わる場合もある
- 例：「良い・普通・悪い」も名義尺度・順序尺度どちらの扱いも可能
- 次は、時間や対象の切り口によるデータの分類を見ていく

**スライド10｜横断面データと時系列データ**
- 横断面データ：ある一時点で複数の対象・複数の変数を測定したデータ
- 例：2019年時点における各都道府県の人口・出生数のデータ
- 時系列データ：特定の変数を時間の経過に沿って観測したデータ
- 例：1995〜2019年の東京都の総人口の推移データ
- どちらもよく使われる代表的なデータの形

**スライド11｜経時データ・コホートデータ・パネルデータ**
- 経時データ：複数の対象を時間・条件を変えて測定したデータ
- コホートデータ：共通の要因を持つ集団を追跡調査したデータ
- 経時データは経済学では「パネルデータ」と呼ばれる
- 医学分野では「繰り返し測定データ」と呼ばれる
- 分野によって同じ考え方でも呼び方が異なる点に注意

**スライド12｜具体例：横断面データと時系列データ**
- 表1.2.1は横断面データと時系列データを並べて示した例
- 横断面データ例：2019年度の各都道府県の人口・出生数
- 時系列データ例：1995〜2019年度の東京都の総人口の推移
- 同じ「人口」というテーマでも、切り口によってデータの形は変わる
- どちらのデータかによって、使える分析手法も変わってくる

**スライド13｜まとめ：1.2.1 種々のデータ**
- データは尺度水準や変数の数などさまざまな観点で特徴づけられる
- 横断面データ・時系列データ・経時データ・コホートデータなど型がある
- 分析の第一歩は「手元のデータがどんな型か」を把握すること
- ここまでが1.2.1の内容
- 次はデータを1つの数値に要約する「代表値」の話に入る

**スライド14｜（切り替え）1.2.2 基本統計量とは**
- データの特性を代表的に表す値を「代表値」または「要約統計量」と呼ぶ
- 大量のデータを1つ、あるいは少数の数値に要約するための指標
- 代表値にはさまざまな種類があり、それぞれ意味や使いどころが異なる
- 今日はまず「データの中心」を表す代表値を学ぶ
- 散らばりを表す指標は次回以降に扱う

**スライド15｜a. 代表値①：算術平均**
- データの中心を表す値として算術平均・幾何平均・調和平均がある
- 算術平均＝すべてのデータを足してデータ数で割った値
- 一般に「平均（値）」と言うと算術平均を指すことが多い
- 最もなじみ深く、直感的に理解しやすい代表値
- 次に、算術平均とは異なる「平均」の考え方を見ていく

**スライド16｜代表値②：幾何平均**
- 幾何平均＝データの積のn乗根で求める平均
- 比率や割合で変化するものの平均を求める際に用いられる
- 例：3年間の販売増加率が+9%、-20%、+20%と変化した場合
- 算術平均ではなく幾何平均を使うことで正しい平均変化率が求まる
- この例では平均の変化率は約1.5%と算出される

**スライド17｜代表値③：調和平均**
- 調和平均＝データの逆数の算術平均の逆数で求める平均
- 速度の平均を求める場面などでよく用いられる
- 例：行きが時速40km、帰りが時速60kmだった場合の平均速度
- 単純な算術平均（50km/h）ではなく調和平均（48km/h）が正しい
- 目的に応じて3種類の平均を使い分けることが重要

**スライド18｜Memo：3つの平均の大小関係**
- データがすべて正の値のとき、調和平均≦幾何平均≦算術平均が成り立つ
- 等号が成立するのは、すべてのデータが等しい場合のみ
- どのようなデータに対しても、この大小関係は一定して成り立つ
- 3つの平均はそれぞれ異なる基準を最小化する値として定義される
- 目的に合わない平均を使うと、誤った解釈につながる可能性がある

**スライド19｜順序統計量・最小値・最大値**
- データを大きさの順に並べ替えたものを「順序統計量」と呼ぶ
- 最も小さい値を「最小値」、最も大きい値を「最大値」と呼ぶ
- 順序統計量を使うことで、他のさまざまな代表値も定義できる
- 次に紹介する中央値・四分位数もこの考え方がベース
- データを並べ替えるという操作がここでのポイント

**スライド20｜中央値と最頻値**
- 中央値＝データを大きさの順に並べたときの「中央」の値
- データ数が偶数か奇数かで中央値の求め方が変わる
- 外れ値（とびぬけた値）がある場合、平均より中央値の方が適切なことが多い
- 最頻値＝データの中で最も多く現れる値
- 名義尺度のような質的データにも使える代表値（ただし、各対象がほぼ一意の値を取る連続的な量的データでは、同じ値を持つ対象が存在せず最頻値が定義できない＝存在しないことが多い点に注意）

**スライド21｜平均値・中央値・最頻値の関係**
- 分布の峰が1つ（単峰性）で左右対称なら、3つの値は一致する
- データに極端に大きい値があると、最頻値≦中央値＜平均値の順になる
- データに極端に小さい値があると、平均値≦中央値＜最頻値の順になる
- つまり平均値は極端な値（外れ値）の影響を受けやすい
- 分布の形が代表値の見え方にも影響することを押さえておく

**スライド22｜四分位数と5数要約**
- データをおおよそ4分の1ずつに区切る位置の値を「四分位数」と呼ぶ
- 第1四分位数・中央値（第2四分位数）・第3四分位数の3つがよく使われる
- 最小値・第1四分位数・中央値・第3四分位数・最大値の5つで
  データの特徴を捉えることを「5数要約」と呼ぶ
- 5数要約はデータ全体を概観する際によく用いられる
- 次回扱う「箱ひげ図」はこの5数要約を視覚化したもの

**スライド23｜データの分布と代表値（イメージ）**
- 分布が対称であれば、平均値・中央値・最頻値はほぼ一致する
- 分布が左右どちらかに偏っていると、3つの値にずれが生じる
- どの代表値を使うかで、データの印象が変わることもある
- そのため複数の代表値をあわせて確認することが望ましい
- グラフによる可視化と組み合わせることでより理解が深まる

**スライド24｜Memo：尺度水準と使える代表値の対応**
- 名義尺度で使える代表値：最頻値
- 順序尺度で使える代表値：中央値（最頻値も使用可）
- 間隔尺度で使える代表値：算術平均（中央値・最頻値も使用可）
- 比率尺度で使える代表値：幾何平均・調和平均（算術平均なども使用可）
- 高い尺度水準ほど、より多様な代表値を計算できる

**スライド25｜まとめと振り返り／次回予告**
- データは尺度水準・変数の数などさまざまな観点で分類される
- 代表値には算術平均・幾何平均・調和平均・中央値・最頻値などがある
- どの代表値が適切かは、データの尺度水準や分布の形による
- 5数要約はデータ全体を概観する基本的な方法
- 次回は「散らばりの指標」（分散・標準偏差など）を扱う

## 演習案（AI活用型／所要時間目安 25〜30分）

**テーマ：都道府県データでAIに代表値を計算させ、解釈する**

1. データの用意（教員側）：教科書の表1.2.1と同系統の題材として、都道府県別の人口・出生数などのデータ（人口推計〔第4表、単位:千人〕と人口動態統計の出生数など）をCSV等で用意し配布する
2. AIへの依頼（10分）：学生は生成AIに「このデータの算術平均・中央値・四分位数を計算して」と依頼し、結果を確認する。あわせて「最頻値も計算できるか」とAIに尋ねさせ、都道府県別人口のような各対象がほぼ一意の値を取るデータでは、通常まったく同じ値を持つ対象が存在せず最頻値が定義できない（存在しない）ことをAIとのやり取りの中で確認する
3. 尺度水準の確認（5分）：「都道府県名」は名義尺度、「人口」は比率尺度など、各列がどの尺度水準かを自分で判断する。最頻値が使える代表値は「名義尺度」のようなカテゴリ的なデータであり、連続的な量的データでは最頻値が意味を持たない（存在しないことが多い）点を、都道府県名の列と人口の列を比較しながら確認する
4. 解釈（5〜10分）：AIに「平均値と中央値に差がある理由を考えて」と尋ね、外れ値（人口の多い都市部など）の影響について考察する
5. 共有（5分）：グループで、どの都道府県が外れ値的な存在だったか、代表値からどんなことが読み取れたかを共有する

**ねらい**
- 尺度水準によって使える代表値が異なることを、実際の計算を通じて体感する
- AIに計算を任せつつ、結果の妥当性や意味を自分で考える姿勢を養う
- 平均値と中央値のずれから、分布の偏りを読み取る練習をする
- 「どんなデータにも常に最頻値が求められるわけではない」ことに気づく

**教員側の準備**
- 演習用データ（都道府県別人口・出生数など）を事前に用意しておく（人口推計〔第4表〕・人口動態統計の出生数データを使用）
